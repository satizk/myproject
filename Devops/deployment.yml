apiVersion: apps/v1
kind: Deployment
metadata:
  name: satizapp
spec:
  selector:
    matchLabels:
      app: satizapp
  template:
    metadata:
      labels:
        app: satizapp
    spec:
      containers:
      - name: myapp
        image: httpd
        resources:
          limits:
            memory: "128Mi"
            cpu: "500m"
        ports:
        - containerPort: 80
